def koaxfr(instr, utdate, directory, tpx=tpx):
	"""
	"""
	
	verify_instrument()
	verify_date(utDate)
	assert directory != '', 'directory value is blank'

	# Transfer to NExScI
	
	# Send email to NExScI
	
	# Update database
	
